clear all; clc; close all;


lena = double(imread('lena.tif'));
M = 3;
[qImage, clusters] = LloydMax(lena, M, 0.001);
lena_reconst = InvLloydMax(qImage, clusters);

PSNR = alcPSNR(lena, lena_reconst, false);

fprintf(1, '', PSNR);